# 面向人和 AI 的起始配置；所有相对路径均以本文件所在目录为基准解析。
module: example_module
description: "用一句话说明模块分析对象、主要方法和公开产物"
input:
  path: data/input.tsv
  id_namespace: gene_symbol
  row_unit: feature
  column_unit: sample
analysis:
  method: replace_with_actual_method
  target: case
  reference: control
  seed: null # 在合同批准固定随机种子后填写；未使用随机过程时保持 null
output:
  directory: result
  result_layout: flat
  figures: []
